
#include <string>
#include <list>
using namespace std;

void print_slist(list<string>, bool, string);
string delete_char(string, char);
string urlth(string, bool);
int get_http_code(char[]);
int isa_main(int, char**);
